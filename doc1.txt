One, sometimes referred unity, first non-zero natural number. thus integer after zero. Any number multiplied by one remains number, one identity multiplication. result, 1 its own factorial, its own square square root, its own cube cube root, so on.